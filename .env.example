# Copy to a local environment loader or export these before starting Nexus.
# Never place real values in a committed configuration file.
NEXUS_CLIENT_TOKEN=replace-with-a-long-random-inbound-token
OPENAI_API_KEY=replace-with-a-provider-api-key
